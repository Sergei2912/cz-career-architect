# 🧪 Как запустить тесты / How to Run Tests

> **Важно!** Все команды выполняются из **корневой директории проекта** (`cz-career-architect/`)

---

## 📋 Оглавление

1. [Быстрый старт](#быстрый-старт)
2. [Подробная инструкция](#подробная-инструкция)
3. [Где выполнять команды](#где-выполнять-команды)
4. [Различные способы запуска](#различные-способы-запуска)
5. [Работа с покрытием кода](#работа-с-покрытием-кода)
6. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрый старт

### Шаг 1: Перейти в корень проекта

```bash
cd /путь/к/проекту/cz-career-architect
```

**Проверьте, что вы в правильной директории:**
```bash
📂 cz-career-architect/  # ← Вы должны быть здесь!
├── base_for_agent_cv/
├── tests/
├── requirements.txt
├── README.md
└── run_tests.sh       # ← Этот файл должен быть виден
```

### Шаг 2: Запустить тесты (самый простой способ)

```bash
./run_tests.sh
```

**Готово!** Скрипт автоматически проверит зависимости и запустит тесты.

---

## 📖 Подробная инструкция

### Предварительные требования

Убедитесь, что установлено:

- ✅ **Python 3.9+** 
  ```bash
  python --version  # Должно быть >= 3.9
  ```

- ✅ **Git**
  ```bash
  git --version
  ```

- ✅ **pip** (менеджер пакетов Python)
  ```bash
  pip --version
  ```

---

### Шаг за шагом

#### 1. Клонировать репозиторий (если еще не клонирован)

```bash
git clone https://github.com/Sergei2912/cz-career-architect.git
cd cz-career-architect
```

**Текущая директория:**
```
📂 /home/user/cz-career-architect  # ← Вы здесь
```

#### 2. Установить зависимости

```bash
📂 /home/user/cz-career-architect
$ pip install -r requirements.txt
```

Или используйте виртуальное окружение (рекомендуется):

```bash
📂 /home/user/cz-career-architect
$ python -m venv venv
$ source venv/bin/activate  # Linux/Mac
$ # или
$ venv\Scripts\activate     # Windows
$ pip install -r requirements.txt
```

#### 3. Настроить переменные окружения

**Опция А: Экспорт в текущей сессии**
```bash
📂 /home/user/cz-career-architect
$ export OPENAI_API_KEY="ваш-ключ-api"
```

**Опция Б: Создать файл .env** (рекомендуется)
```bash
📂 /home/user/cz-career-architect
$ cp .env.example .env
$ nano .env  # Отредактировать и добавить свой API ключ
```

Содержимое `.env`:
```
OPENAI_API_KEY=sk-your-actual-api-key-here
API_BASE_URL=https://api.openai.com/v1
```

#### 4. Запустить тесты

```bash
📂 /home/user/cz-career-architect  # ← ВАЖНО: в корне проекта!
$ pytest tests/ -v
```

---

## 📍 Где выполнять команды?

### ✅ Правильно - из корня проекта:

```
📂 /home/user/cz-career-architect/  # ← ЗДЕСЬ! ✓
├── base_for_agent_cv/
│   └── src/
├── tests/                          # ← Тесты здесь
│   ├── test_config.py
│   └── ...
├── requirements.txt
└── run_tests.sh                    # ← Скрипт здесь
```

**Команда:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -v  # ✓ Правильно!
```

### ❌ Неправильно - из других директорий:

```bash
📂 /home/user/cz-career-architect/tests
$ pytest -v  # ✗ Будут ошибки импорта!
```

```bash
📂 /home/user/cz-career-architect/base_for_agent_cv
$ pytest ../tests/ -v  # ✗ Неправильные пути!
```

### 💡 Как проверить, что вы в правильной директории?

```bash
# Команда для проверки
pwd

# Должно показать что-то вроде:
# /home/user/cz-career-architect
# или
# /Users/user/Projects/cz-career-architect
# или
# C:\Users\user\cz-career-architect

# Также проверьте наличие ключевых файлов:
ls
# Должны видеть: tests/, base_for_agent_cv/, requirements.txt, run_tests.sh
```

---

## 🛠️ Различные способы запуска

### Способ 1: Использовать готовый скрипт (рекомендуется для новичков)

```bash
📂 /home/user/cz-career-architect
$ ./run_tests.sh
```

**Возможности скрипта:**
- Проверяет зависимости
- Активирует виртуальное окружение (если есть)
- Предупреждает о недостающих настройках
- Красивый вывод с эмодзи

**Дополнительные опции:**
```bash
$ ./run_tests.sh coverage  # С покрытием кода
$ ./run_tests.sh quick     # Быстрый запуск
$ ./run_tests.sh file test_config  # Конкретный файл
```

### Способ 2: Использовать Make (для разработчиков)

```bash
📂 /home/user/cz-career-architect
$ make test              # Запустить все тесты
$ make test-coverage     # С покрытием (HTML отчет)
$ make test-quick        # Быстрый запуск
$ make test-file FILE=test_config  # Конкретный файл
```

### Способ 3: Прямой вызов pytest (для продвинутых)

**Все тесты с подробным выводом:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -v
```

**Конкретный файл:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/test_config.py -v
```

**Конкретный тест:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/test_config.py::TestSettings::test_settings_default_values -v
```

**Быстрый запуск (без вывода):**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -q
```

**С остановкой на первой ошибке:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -x
```

**Показать локальные переменные при ошибке:**
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -v --showlocals
```

---

## 📊 Работа с покрытием кода

### Простой отчет в терминале

```bash
📂 /home/user/cz-career-architect
$ pytest tests/ --cov=base_for_agent_cv --cov-report=term-missing
```

**Вывод будет примерно таким:**
```
Name                              Stmts   Miss  Cover   Missing
---------------------------------------------------------------
base_for_agent_cv/src/config.py      45      1    98%   123
base_for_agent_cv/src/exceptions.py  38      0   100%
...
---------------------------------------------------------------
TOTAL                              1234    123    90%
```

### HTML отчет (с графиками)

```bash
📂 /home/user/cz-career-architect
$ pytest tests/ --cov=base_for_agent_cv --cov-report=html
```

Затем откройте отчет:
```bash
📂 /home/user/cz-career-architect
$ open htmlcov/index.html  # Mac
$ xdg-open htmlcov/index.html  # Linux
$ start htmlcov/index.html  # Windows
```

### Используя Make

```bash
📂 /home/user/cz-career-architect
$ make test-coverage
```

Автоматически создаст HTML отчет в `htmlcov/`.

---

## 🔧 Решение проблем

### Проблема 1: "No module named 'pytest'"

**Причина:** pytest не установлен

**Решение:**
```bash
📂 /home/user/cz-career-architect
$ pip install pytest pytest-cov pytest-mock
# или
$ pip install -r requirements.txt
```

### Проблема 2: "ModuleNotFoundError: No module named 'base_for_agent_cv'"

**Причина:** Запускаете из неправильной директории

**Решение:**
```bash
# Проверьте текущую директорию
$ pwd

# Перейдите в корень проекта
$ cd /path/to/cz-career-architect

# Убедитесь, что видите нужные файлы
$ ls
# Должны увидеть: base_for_agent_cv/, tests/, requirements.txt
```

### Проблема 3: "ModuleNotFoundError: No module named 'openai'"

**Причина:** Не все зависимости установлены

**Решение:**
```bash
📂 /home/user/cz-career-architect
$ pip install openai anthropic email-validator httpx
# или
$ pip install -r requirements.txt
```

### Проблема 4: "OPENAI_API_KEY not set" или похожие предупреждения

**Причина:** Переменная окружения не установлена

**Решение А: Экспорт в текущей сессии**
```bash
📂 /home/user/cz-career-architect
$ export OPENAI_API_KEY="sk-your-key-here"
$ pytest tests/ -v
```

**Решение Б: Создать .env файл**
```bash
📂 /home/user/cz-career-architect
$ cat > .env << EOF
OPENAI_API_KEY=sk-your-actual-key-here
API_BASE_URL=https://api.openai.com/v1
EOF
```

### Проблема 5: "Permission denied: ./run_tests.sh"

**Причина:** Файл не имеет прав на выполнение

**Решение:**
```bash
📂 /home/user/cz-career-architect
$ chmod +x run_tests.sh
$ ./run_tests.sh
```

### Проблема 6: Тесты проходят, но с предупреждениями (warnings)

**Причина:** Устаревший синтаксис Pydantic (v1 → v2)

**Решение:** Это нормально! Предупреждения не критичны:
```
27 warnings (PydanticDeprecatedSince20)
```
Можно игнорировать или исправить в будущем.

### Проблема 7: "Connection refused" или ошибки Redis/Cache

**Причина:** Redis не запущен (необязательный)

**Решение:** Кеширование опционально. Тесты должны работать без Redis:
```bash
📂 /home/user/cz-career-architect
$ pytest tests/ -v  # Работает без Redis
```

Если нужен Redis:
```bash
# Linux/Mac
$ sudo service redis-server start
# или
$ redis-server

# Docker
$ docker run -d -p 6379:6379 redis:alpine
```

---

## 📚 Дополнительная информация

### Структура тестов

```
tests/
├── conftest.py              # Общие фикстуры
├── test_analysis.py         # Тесты анализа текста
├── test_api.py              # Тесты API
├── test_config.py           # Тесты конфигурации
├── test_exceptions.py       # Тесты исключений
├── test_logging.py          # Тесты логирования
├── test_validator.py        # Тесты валидации (старые)
└── test_validators.py       # Тесты валидаторов (новые)
```

### Количество тестов

- **Всего:** 107 тестов
- **Конфигурация:** 9 тестов
- **Исключения:** 13 тестов
- **Логирование:** 8 тестов
- **Валидаторы:** 67 тестов
- **API:** 3 теста
- **Анализ:** 3 теста
- **Входные данные:** 4 теста

### Покрытие кода

- **Общее:** ~36%
- **Основные модули:** 98-100%
  - `src/config.py` - 98%
  - `src/exceptions.py` - 100%
  - `src/logging_config.py` - 98%
  - `src/validators.py` - 99%

### CI/CD

Тесты автоматически запускаются в GitHub Actions:
- При каждом push
- При каждом pull request
- На нескольких версиях Python (3.9, 3.10, 3.11)

Конфигурация: `.github/workflows/ci.yml`

---

## 💡 Советы

1. **Используйте виртуальное окружение** для изоляции зависимостей
2. **Запускайте тесты перед коммитом** для проверки изменений
3. **Используйте `-v` флаг** для подробного вывода
4. **Проверяйте покрытие** для поиска непротестированного кода
5. **Используйте `-k` для фильтрации** тестов по названию
   ```bash
   pytest tests/ -k "config" -v  # Только тесты с "config" в названии
   ```

---

## 📞 Поддержка

Если у вас остались вопросы:

1. **Документация проекта:** [README_RU.md](README_RU.md)
2. **Детали тестов:** [TEST_RESULTS.md](TEST_RESULTS.md)
3. **GitHub Issues:** [Создать issue](https://github.com/Sergei2912/cz-career-architect/issues)

---

## ✅ Чек-лист перед запуском тестов

- [ ] Я нахожусь в корневой директории проекта (`cz-career-architect/`)
- [ ] Python 3.9+ установлен (`python --version`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Переменная OPENAI_API_KEY установлена (опционально)
- [ ] Я вижу файлы `tests/`, `base_for_agent_cv/`, `run_tests.sh`

**Теперь можно запускать:**
```bash
📂 /home/user/cz-career-architect
$ ./run_tests.sh
# или
$ pytest tests/ -v
# или
$ make test
```

---

**Удачи! 🚀**
